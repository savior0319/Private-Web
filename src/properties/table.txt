CREATE TABLE USERINFO(
USERID VARCHAR2(30) PRIMARY KEY,
USERPWD CHAR(64) NOT NULL,
USERNAME VARCHAR2(30) NOT NULL,
USERGENDER NCHAR(1) NOT NULL,
USERLOCATION VARCHAR2(30) NOT NULL,
USERMAIL VARCHAR2(30) NOT NULL UNIQUE,
ENTERDATE DATE DEFAULT(SYSDATE)
);